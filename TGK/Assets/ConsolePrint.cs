using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConsolePrint : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
         Debug.Log("PLS kill me");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
